import React from 'react';
import ReactDOM from 'react-dom/client';
import FormValid from './FormValid';


const aadarsh = ReactDOM.createRoot(document.getElementById('aadarsh'));
aadarsh.render(
    <React.StrictMode>
     <FormValid />
    </React.StrictMode>
);

